@model Instructor


<div class="modal fade" id="ins-del-approve-@Model.InstructorId" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Onay</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body text-start">
        <b>@Model.InstructorName</b> isimli öğretim üyesi silinecektir! <br> Onaylıyor musunuz ?
      </div>
      <div class="modal-footer">
    <form asp-action="Delete" method="post">
        <input type="hidden" value="@Model.InstructorId" name="id">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">İptal</button>
        <button type="submit" class="btn btn-danger">Sil</button>
    </form>
      </div>
    </div>
  </div>
</div>